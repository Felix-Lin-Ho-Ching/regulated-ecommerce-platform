import { Field } from "./form-controls";
export function ProductBasicSection({ name, setName, brand, setBrand, isEditing, setSkuManual }: { name: string; setName: (value: string) => void; brand: string; setBrand: (value: string) => void; isEditing: boolean; setSkuManual: (value: boolean) => void; }) { return <><Field label="Product name" name="name" value={name} onChange={(event) => setName(event.target.value)} placeholder="StunFry Compact Safety Device" hint="Shown on the product page, cart, checkout, and admin orders." required/><Field label="Brand" name="brand" value={brand} onChange={(event) => { setBrand(event.target.value); if (!isEditing) setSkuManual(false); }} placeholder="Stun Fry" hint="Brand shown to customers and admins."/></>; }
