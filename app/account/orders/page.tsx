import Link from "next/link";import { AppShell, SectionHeader, StatusBadge } from "@/components/ui";import { orders } from "@/lib/mock-data";
export default function Orders(){return <AppShell><SectionHeader eyebrow="Orders" title="Order history"/><div className="grid gap-3">{orders.map(o=><Link className="card flex justify-between p-4" href={`/account/orders/${o.id}`} key={o.id}><span><strong>{o.id}</strong><br/><span className="text-sm text-slate-600">{o.title}</span></span><StatusBadge tone={o.payment==="paid"?"success":o.payment==="failed"?"danger":"warning"}>{o.payment}</StatusBadge></Link>)}</div></AppShell>}
