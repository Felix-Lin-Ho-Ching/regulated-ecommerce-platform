import { AdminShell, SectionHeader, AlertPanel } from "@/components/ui";
export default function Page(){return <AdminShell title="payment settings"><SectionHeader eyebrow="Admin module" title="Payment settings">Configured through environment variables, not editable here.</SectionHeader><AlertPanel title="Environment-only configuration" tone="info">Payment provider credentials and live/mock mode are configured through environment variables, not editable here. This page is an explicit launch classification and does not claim mock settings are launch-ready.</AlertPanel></AdminShell>}
