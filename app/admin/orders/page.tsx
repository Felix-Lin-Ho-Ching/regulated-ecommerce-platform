import Link from "next/link";import { AdminShell, AdminDataTable, StatusBadge } from "@/components/ui";import { orders } from "@/lib/mock-data";
export default function OrdersAdmin(){return <AdminShell title="Orders"><AdminDataTable columns={["Order","Customer","Payment","Verification","Fulfillment","Action"]} rows={orders.map(o=>[o.id,o.customer,<StatusBadge key={o.id} tone={o.payment==="paid"?"success":o.payment==="failed"?"danger":"warning"}>{o.payment}</StatusBadge>,o.verification,o.fulfillment,<Link key={o.id} href={`/admin/orders/${o.id}`}>Review</Link>])}/></AdminShell>}
