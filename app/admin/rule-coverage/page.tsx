import { AdminShell, RuleCoverageMatrix, SectionHeader } from "@/components/ui";import { getRuleCoverageRows } from "@/lib/db/catalog";
export default async function Coverage(){const rows=await getRuleCoverageRows();return <AdminShell title="Rule coverage"><SectionHeader eyebrow="Launch readiness" title="Rule coverage matrix">Missing or unverified coverage blocks owner launch for restricted categories. Phase 1 defaults restricted states to manual review.</SectionHeader><RuleCoverageMatrix rows={rows}/></AdminShell>}
