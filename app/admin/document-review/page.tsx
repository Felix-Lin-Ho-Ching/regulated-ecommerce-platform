import { AdminShell, AdminDataTable, FormField, AlertPanel } from "@/components/ui";import { documentReviews } from "@/lib/mock-data";
export default function DocReview(){return <AdminShell title="Document review"><AdminDataTable columns={["Document","Order","Customer","Type","Status","Age"]} rows={documentReviews.map(d=>[d.id,d.order,d.customer,d.type,d.status,d.age])}/><section className="card mt-4 p-5"><h2 className="font-black">Approval/rejection notes</h2><FormField label="Required reviewer note" value="Name and address match order."/><div className="mt-4"><AlertPanel title="Privacy reminder" tone="warning">Document storage and retention are mocked. Backend design needs a formal privacy model.</AlertPanel></div></section></AdminShell>}
