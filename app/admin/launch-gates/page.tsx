import { AdminShell, LaunchGateCard, SectionHeader } from "@/components/ui";export default function Gates(){return <AdminShell title="Launch gates"><SectionHeader eyebrow="Owner launch gate" title="Blocked, ready, and enabled gates">Owner-only actions are marked and remain disabled when blockers exist.</SectionHeader><LaunchGateCard/></AdminShell>}
