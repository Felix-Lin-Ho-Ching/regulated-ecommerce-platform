import { AdminShell, AdminDataTable, FormField, AlertPanel } from "@/components/ui";import { complianceRules } from "@/lib/mock-data";
export default function Rules(){return <AdminShell title="Compliance rules"><AdminDataTable columns={["Rule","State","Category","Outcome","Coverage","Note"]} rows={complianceRules.map(r=>[r.id,r.state,r.category,r.outcome,r.coverage,r.note])}/><section className="card mt-4 p-5"><h2 className="font-black">Rule change note</h2><FormField label="Required change note" value="Updated after counsel review."/><AlertPanel title="No legal automation" tone="info">This workflow uses mock legal-rule content until counsel-approved rules are added.</AlertPanel></section></AdminShell>}
