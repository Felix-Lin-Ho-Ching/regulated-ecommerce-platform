import { AdminShell, AdminDataTable, StatusBadge, FormField } from "@/components/ui";import { products } from "@/lib/mock-data";
export default function Inventory(){return <AdminShell title="Inventory"><AdminDataTable columns={["SKU","Product","Available","Reserved","Restricted","Last adjustment"]} rows={products.map(p=>[p.sku,p.name,p.stock,Math.min(4,p.stock),<StatusBadge key={p.id} tone={p.restricted?"warning":"success"}>{p.restricted?"restricted":"standard"}</StatusBadge>,"Reason required"])} /><section className="card mt-4 p-5"><h2 className="font-black">Stock adjustment</h2><FormField label="Reason" value="Cycle count correction"/></section></AdminShell>}
