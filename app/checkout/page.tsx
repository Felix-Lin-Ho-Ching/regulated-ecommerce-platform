import Link from "next/link";import { AppShell, CheckoutStepper, CheckoutSummary, ComplianceResultPanel, SectionHeader } from "@/components/ui";import type { CheckoutOutcome } from "@/lib/mock-data";
export default function Checkout(){return <AppShell><SectionHeader eyebrow="Checkout" title="Step-based checkout prototype">Use the links below to inspect every mock compliance outcome.</SectionHeader><CheckoutStepper active={1}/><div className="mt-6 grid gap-6 md:grid-cols-[1fr_320px]"><section className="grid gap-3">{["allowed","blocked","pending_admin_review","pending_document_upload","ready_for_payment","payment_failed","paid"].map(o=><div className="card p-4" key={o}><ComplianceResultPanel outcome={o as CheckoutOutcome}/><Link className="btn btn-secondary mt-3" href={`/checkout/verification?case=${o}`}>View {o}</Link></div>)}</section><CheckoutSummary/></div></AppShell>}
